
# Implement the following plan
Follow the `Instructions` to implement the `Plan`, log progress, then `Report` the completed work.

## Instructions
- Read the ENTIRE plan file first
- Check the Acceptance Criteria section - these are REQUIRED outcomes
- Execute ALL steps in "Step by Step Tasks" section in order
- Do NOT skip steps, especially UI/frontend steps
- If acceptance criteria includes UI features, UI implementation is MANDATORY
- Continue until ALL steps complete

## Plan
$ARGUMENTS

## Report
- Summarize the work you've just done in a concise bullet point list
- Report the files and total lines changed with `git diff --stat`
- Confirm log file created at: `{plan_directory}/{plan_name}_implementation.log`